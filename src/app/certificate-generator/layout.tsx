import { getToolMetadata } from '../lib/seo';
export const metadata = getToolMetadata('certificate-generator');
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
